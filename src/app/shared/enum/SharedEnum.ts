export enum NotifierType {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info'
}

export enum SessionAtribute {
  USER = 'user'
}

export enum ExchangeAction {
  BUY = 'buy',
  SELL = 'sell',
  SOLD = 'sold'
}

export enum ExchangeStatus {
  WAITING = 'waiting',
  DONE = 'done',
  AVOID = 'avoid'
}

export enum Mode {
  NEW = 'new',
  CLONE_REQUEST = 'clone_request'
}

export enum DataType {
  BLOCK = 'block',
  BLOCKCHAIN = 'blockchain',
  TRANSACTION = 'transaction'
}
